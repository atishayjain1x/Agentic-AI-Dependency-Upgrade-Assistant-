services:
  postgres:
    image: postgres:16
    container_name: dependency-agent-postgres
    environment:
      POSTGRES_DB: dependency_agent
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
